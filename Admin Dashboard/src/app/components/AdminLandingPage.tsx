/* This file is no longer used — the Landing page has been merged into AdminCohortDashboard (Dashboard screen).
   Kept as a stub to avoid breaking any lingering imports. */

export function AdminLandingPage() {
  return null;
}
